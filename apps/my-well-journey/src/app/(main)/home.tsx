import { View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { CoreText } from '@param-nx-platform/core-ui';

export default function HomeScreen() {
  return (
    <SafeAreaView>
      <CoreText>Home1</CoreText>
    </SafeAreaView>
  );
}
